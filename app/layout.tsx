import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })
const playfair = Playfair_Display({ subsets: ['latin'], variable: '--font-playfair' })

export const metadata: Metadata = {
  title: 'Abdul Mannan - Electrical Engineering Portfolio',
  description: 'Portfolio of Abdul Mannan - Electrical Engineering Student specializing in Power Systems, Circuit Design, and IoT',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} ${playfair.variable} font-sans bg-cream-50 text-charcoal-900`}>
        {children}
      </body>
    </html>
  )
}
